import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'G1_KinaAlex_Uebungen',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
